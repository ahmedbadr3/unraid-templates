<?xml version="1.0"?>
<Container version="2">
  <Name>print-blockage-stopper</Name>
  <Repository>abadrdh/print-blockage-stopper</Repository>
  <Registry>https://hub.docker.com/r/abadrdh/print-blockage-stopper</Registry>
  <Branch>
    <Tag>latest</Tag>
    <TagDescription>Latest stable release</TagDescription>
  </Branch>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <ExtraParams>--cap-drop=ALL --cap-add=DAC_OVERRIDE --cap-add=CHOWN --cap-add=FOWNER --cap-add=SETUID --cap-add=SETGID --security-opt=no-new-privileges</ExtraParams>
  <Support>https://forums.unraid.net/topic/197898-support-print-blockage-stopper-automated-print-head-maintenance/</Support>
  <Project>https://github.com/ahmedbadr3/print-blockage-stopper</Project>
  <Overview>
    Automated maintenance prints for any network printer (especially pigment printers like Canon imagePROGRAF, Epson SureColor, HP DesignJet, etc.).

    Sends a small test image on a configurable schedule (default: every 3 days) to keep all ink channels flowing and prevent costly pigment clogs and cleaning cycles.

    Features: multi-printer support, network printer discovery, per-printer schedules, smart skip (won't print if the printer was recently used), preset test images for 4/6/8/11/12-colour printers, custom image upload, and a built-in dashboard.
  </Overview>
  <Category>Productivity: Tools:</Category>
  <WebUI>http://[IP]:[PORT:8631]</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/ahmedbadr3/unraid-templates/main/print-blockage-stopper.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/ahmedbadr3/print-blockage-stopper/main/icon.png</Icon>
  <Description>
    Keeps any network printer's print head healthy by automatically sending a small test print on a schedule. Prevents pigment ink clogs without wasting ink on full cleaning cycles. Supports multiple printers, auto-discovery, custom test images, and smart scheduling. Works with Canon imagePROGRAF, Epson SureColor, HP DesignJet, and other IPP Everywhere compatible printers.
  </Description>
  <DateInstalled></DateInstalled>
  <DonateText>Buy me a coffee</DonateText>
  <DonateLink></DonateLink>
  <Requires>Any network printer with IPP Everywhere support (usually any modern networked printer). Especially useful for pigment printers.</Requires>
  <Beta>false</Beta>
  <Changes>
    ### 1.4.1
    - Next scheduled print countdown on each printer card
    - Custom test image delete button
    - Favicon for browser tab
    - New app icon
    - Security hardening (CSRF protection, XSS fixes, SSRF prevention, Content-Security-Policy)
    - Fixed chart alignment in print history
    - Fixed thumbnail not updating when changing test image
    - Replaced deprecated cgi module, use threaded HTTP server
    - CUPS config now persists across container restarts
    - Portable grep for broader Linux compatibility
    ### 1.4.0
    - Inline printer rename (double-click name to edit)
    - Test Connection button with auto-detect printer model
    - Real-time connection status indicators (green/red dots)
    - Ink level bars (via IPP marker attributes)
    - Print retry logic (auto-retry once after 30s on failure)
    - Webhook notifications for print success/failure (Slack, Discord, ntfy)
    - CSV export for print history
    - Test image thumbnails on printer cards
    - Mobile-responsive layout improvements
    ### 1.3.0
    - Multi-printer support (add/remove from dashboard)
    - Network printer auto-discovery (mDNS/IPP)
    - Per-printer status, schedule, and skip-hours
    - Preset test images for 4/6/8/11/12-colour printers
    - Custom test image upload (max 5 MB)
    - Smart skip: won't print if printer was recently used
    - Dashboard schedule pause/resume toggle
    - 30-day print history chart
    ### 1.0.0
    - Initial release
  </Changes>

  <!-- ── Ports ────────────────────────────────────────────── -->
  <Config
    Name="Dashboard"
    Target="8631"
    Default="8631"
    Mode="tcp"
    Description="Web dashboard for managing printers, schedules, and test images."
    Type="Port"
    Display="always"
    Required="true"
    Mask="false">8631</Config>

  <Config
    Name="CUPS Web UI"
    Target="631"
    Default="631"
    Mode="tcp"
    Description="CUPS print server interface for advanced monitoring."
    Type="Port"
    Display="advanced"
    Required="false"
    Mask="false">631</Config>

  <!-- ── Environment Variables ────────────────────────────── -->
  <Config
    Name="Printer IP Address"
    Target="PRINTER_IP"
    Default=""
    Description="(Optional) IP address of a printer to auto-add on first boot. You can also add printers from the dashboard after starting the container."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Default Print Schedule"
    Target="SCHEDULE"
    Default="0 10 */3 * *"
    Description="Default cron expression for new printers. Can be customised per-printer in the dashboard. Default: every 3 days at 10 AM."
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">0 10 */3 * *</Config>

  <Config
    Name="Default Paper Size"
    Target="PAPER_SIZE"
    Default="A4"
    Description="Default paper size for new printers (e.g., A4, Letter, A3). Can be changed per-printer."
    Type="Variable"
    Display="always"
    Required="true"
    Mask="false">A4</Config>

  <Config
    Name="Default Skip Hours"
    Target="SKIP_HOURS"
    Default="72"
    Description="Default hours to skip maintenance print if printer was recently used. Adjustable per-printer in the dashboard."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">72</Config>

  <Config
    Name="Connection Type"
    Target="CONNECTION"
    Default="ipp"
    Description="Default connection type for new printers: 'ipp' (recommended) or 'socket' (port 9100)."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">ipp</Config>

  <Config
    Name="Webhook URL"
    Target="WEBHOOK_URL"
    Default=""
    Description="(Optional) URL for print success/failure notifications. Works with Slack incoming webhooks, Discord webhooks, ntfy.sh, or any endpoint accepting JSON POST."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Printer Port"
    Target="PRINTER_PORT"
    Default="9100"
    Description="Default port for socket connections (only used when Connection Type is 'socket')."
    Type="Variable"
    Display="advanced"
    Required="false"
    Mask="false">9100</Config>

  <!-- ── Volumes ──────────────────────────────────────────── -->
  <Config
    Name="Data / Logs"
    Target="/data"
    Default="/mnt/user/appdata/print-blockage-stopper"
    Description="Persistent storage for printer config, logs, uploads, and CUPS state."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/print-blockage-stopper</Config>

  <Config
    Name="Unraid Notifications"
    Target="/unraid/notify"
    Default="/usr/local/emhttp/webGui/scripts/notify"
    Description="Mount Unraid's notification script to get alerts on print failures. Leave default for standard Unraid installs."
    Type="Path"
    Display="advanced"
    Required="false"
    Mask="false">/usr/local/emhttp/webGui/scripts/notify</Config>
</Container>
